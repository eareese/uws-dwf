{
  "name": "uws-dwf",
  "version": "0.0.0",
  "description": "uws dwf",
  "main": "index.js",
  "scripts": {
    "build": "npm run build-html && npm run build-css && webpack -p",
    "build-html": "cp src/index.html public/",
    "build-css": "cp src/main.css public/",
    "clean-all": "rm -rf public && mkdir public",
    "dev": "npm run clean-all && npm run build-html && npm run build-css && webpack-dev-server",
    "start": "http-server",
    "test": "standard"
  },
  "author": "Emily Reese <emilyreese@fastmail.com>",
  "license": "ISC",
  "engines": {
    "node": "5.9.1"
  },
  "devDependencies": {
    "babel-core": "^6.22.1",
    "babel-loader": "^6.2.10",
    "babel-preset-es2015": "^6.22.0",
    "standard": "^8.6.0",
    "webpack": "^2.2.1",
    "webpack-dev-server": "^2.3.0"
  },
  "dependencies": {
    "canvg-browser": "^1.0.0",
    "http-server": "^0.9.0",
    "konva": "^1.4.0"
  }
}
